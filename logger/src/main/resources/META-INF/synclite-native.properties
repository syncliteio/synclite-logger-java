# Substituted at build time by maven-resources-plugin filtering.
# Consumed by io.synclite.consolidator.NativeLoader to compose the
# canonical JNI library file name inside the jar
# (META-INF/native/<classifier>/libsynclite_<revision>_jni.<ext>).
jni.revision=${revision}
